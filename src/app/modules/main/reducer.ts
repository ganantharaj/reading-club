// This file is where the entire state management for Main page happens.

export const initialState = {};

export const reducer = (state: any, action: any): unknown => {
  switch (action.type) {
    default:
      return state;
  }
};
